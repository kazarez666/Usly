# Usly photo screen

`/widget/` is a Locket-style web photo screen. It uses the latest-photo value stored by the app in `localStorage` (`usly-latest-photo-<coupleId>`).

A true iOS WidgetKit / Android AppWidget home-screen widget still requires a native mobile target; this web screen is the browser/PWA-compatible part and can be opened as a standalone shortcut.
